import 'package:test/test.dart';
import 'package:carnevale_api/carnevale_api.dart';

// tests for TransformEntryInput
void main() {
  final instance = TransformEntryInputBuilder();
  // TODO add properties to the builder and call build()

  group(TransformEntryInput, () {
    // The desired form (true = the model's alternate card, false = the one it was hired as) rather than a toggle, so a retried request can't flip it back. 
    // bool transformed
    test('to test the property `transformed`', () async {
      // TODO
    });

  });
}
