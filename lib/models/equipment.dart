// Superseded by the generated `api.Equipment` (F-P2-1) — the hand-written Equipment model and its
// converter were removed. This now-empty file can be deleted (`git rm lib/models/equipment.dart`);
// it is retained only because the sandbox blocked file deletion in this session.
