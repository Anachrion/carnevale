// Superseded by the generated api.ModelList / api.ListEntry / api.Spell (F-P2-1) — the hand-written
// Gang/ListEntry/Spell models and their converters were removed. This now-empty file can be deleted
// (`git rm lib/models/gang.dart`); it is retained only because the sandbox blocked file deletion.
